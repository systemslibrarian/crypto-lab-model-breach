{
  "name": "crypto-lab-model-breach",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "tsx scripts/check-roundtrip.ts && tsx scripts/check-theorem1.ts && tsx scripts/check-attack.ts",
    "check": "npm run typecheck && npm test"
  },
  "devDependencies": {
    "tsx": "^4.21.0",
    "typescript": "~6.0.2",
    "vite": "^8.0.4"
  }
}
